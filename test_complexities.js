const { RouterLogic } = require('./out/proxy/RouterLogic.js');
const { ModelTiering } = require('./out/proxy/ModelTiering.js');

// If the TS code isn't compiled to CommonJS or ES modules, we can use the test file which has it all inline.
